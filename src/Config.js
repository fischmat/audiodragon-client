
export const appConfig = {
    backend: {
        protocol: 'http',
        host: 'localhost',
        port: '8080',
        apiPath: '',

        websocket: {
            endpoint: '/events'
        }
    }
}